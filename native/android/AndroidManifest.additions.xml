<!-- Merge this permission into the Android app manifest. It opens a user-controlled
     Usage Access switch in Android Settings; it is not a runtime permission. -->
<uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
